Belongs to the directory "R"

"V1" contains old .R files

"installingPackages.R" contains the code used for installing all necessary packages from CRAN/github

"login_credentials.R" contains a couple of lines of code visualising how I'd go about hiding my login credentials when they're absolutely necessary in a file.